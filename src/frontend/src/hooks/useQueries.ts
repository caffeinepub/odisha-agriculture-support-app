import { useQuery } from '@tanstack/react-query';
import { useActor } from './useActor';

// This file is intentionally minimal as feature-specific hooks are in separate files
// following the implementation plan structure
